version: '3'

services:
  minio:
    image: minio/minio:RELEASE.2021-06-17T00-10-46Z
    container_name: ${CONTAINER_NAME}
    environment:
      - MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY}
      - MINIO_SECRET_KEY=${MINIO_SECRET_KEY}
    command:
      server /data
    volumes:
      - s3-disk:/data
    networks:
      - minio
      - proxy-net
    labels:
      #### Labels define the behavior and rules of the traefik proxy for this container ####
      - "traefik.enable=true" # <== Enable traefik to proxy this container
      - "traefik.http.routers.${PROJECT_NAME}-minio.rule=Host(`minio.${PROJECT_NAME}.ted-data.eu`)" # <== Your Domain Name goes here for the http rule
      - "traefik.http.routers.${PROJECT_NAME}-minio.entrypoints=web" # <== Defining the entrypoint for http, **ref: line 30
      - "traefik.http.routers.${PROJECT_NAME}-minio.middlewares=redirect@file" # <== This is a middleware to redirect to https
      - "traefik.http.routers.${PROJECT_NAME}-minio-secured.rule=Host(`minio.${PROJECT_NAME}.ted-data.eu`)" # <== Your Domain Name for the https rule
      - "traefik.http.routers.${PROJECT_NAME}-minio-secured.entrypoints=web-secured" # <== Defining entrypoint for https, **ref: line 31
      - "traefik.http.routers.${PROJECT_NAME}-minio-secured.tls.certresolver=mytlschallenge" # <== Defining certsresolvers for https


volumes:
  s3-disk:


networks:
  minio:
    internal: true
  proxy-net:
    external:
      name: proxy-net