version: "3.8"
services:
  mongodb:
    image: mongo
    container_name: mongodb-${ENVIROMENT}
    environment:
        - MONGO_INITDB_ROOT_USERNAME=root
        - MONGO_INITDB_ROOT_PASSWORD=pass12345
    volumes:
      - mongodb-data:/data/db
    networks:
      - mongodb-net
      - proxy-net
#    ports:
#      - 27017:27017
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongo mongodb-${ENVIROMENT}:27017/test --quiet
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped


  mongo-express:
    image: mongo-express
    container_name: mongo-express-${ENVIROMENT}
    environment:
      - ME_CONFIG_MONGODB_SERVER=mongodb
      - ME_CONFIG_MONGODB_ENABLE_ADMIN=true
      - ME_CONFIG_MONGODB_ADMINUSERNAME=root
      - ME_CONFIG_MONGODB_ADMINPASSWORD=pass12345
      - ME_CONFIG_BASICAUTH_USERNAME=admin
      - ME_CONFIG_BASICAUTH_PASSWORD=admin123
      - ME_CONFIG_MONGODB_URL=mongodb://root:pass12345@mongodb-${ENVIROMENT}:27017/

    volumes:
      - mongodb-data:/data/db
    depends_on:
      - mongodb
    networks:
      - mongodb-net
      - proxy-net
    ports:
      - 8081:8081
    healthcheck:
      test: wget --quiet --tries=3 --spider http://admin:admin123@localhost:8081 || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped
    labels:
      #### Labels define the behavior and rules of the traefik proxy for this container ####
      - "traefik.enable=true" # <== Enable traefik to proxy this container
      - "traefik.http.routers.${ENVIROMENT}-mongo.rule=Host(`mongo.${SUBDOMAIN}${DOMAIN}`)" # <== Your Domain Name goes here for the http rule
      - "traefik.http.routers.${ENVIROMENT}-mongo.entrypoints=web" # <== Defining the entrypoint for http, **ref: line 30
      - "traefik.http.routers.${ENVIROMENT}-mongo.middlewares=redirect@file" # <== This is a middleware to redirect to https
      - "traefik.http.routers.${ENVIROMENT}-mongo-secured.rule=Host(`mongo.${SUBDOMAIN}${DOMAIN}`)" # <== Your Domain Name for the https rule
      - "traefik.http.routers.${ENVIROMENT}-mongo-secured.entrypoints=web-secured" # <== Defining entrypoint for https, **ref: line 31
      - "traefik.http.routers.${ENVIROMENT}-mongo-secured.tls.certresolver=mytlschallenge" # <== Defining certsresolvers for https


volumes:
  mongodb-data:
    name: mongodb-data-${ENVIROMENT}

networks:
  mongodb-net:
    internal: true
    name: mongodb-net-${ENVIROMENT}
  proxy-net:
    external:
      name: proxy-net
